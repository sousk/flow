defaults: &defaults
  host: localhost
  # slave_one:
  #   port: 27018
  # slave_two:
  #   port: 27019

development:
  <<: *defaults
  database: flow

production:
  <<: *defaults
  host: mongodb.example.com
  username: user
  password: pass
  database: flow
